import React from 'react'

const CharactersList = ([characters]) => {

    const characterItems = movies.characters.map((character) => {
        return key={character}
    })
  return (

    <div>CharactersList</div>

  )

}

export default CharactersList